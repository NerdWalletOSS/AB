We use the following convention to perform quick and dirty tests of utility functions. As an example, 
From the command line
export ENABLE_PHP_CLI=TRUE
unset ENABLE_PHP_CLI
=======================================
We have a number of utility functions that access the database. 
They do not have much business logic in them. They are prefixed with db_
======================================
